// undeclared is a variable that doesnt exist at all.
let undef;
const user = {
    first: "rich"
};

console.log(undef);
console.log(user.last);

// undefined
//console.log(example); // jshint ignore:line